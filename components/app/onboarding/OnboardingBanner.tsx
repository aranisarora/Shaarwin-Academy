/** Slim "finishing setup" strip shown on booking pages reached from onboarding. */
export function OnboardingBanner() {
  return (
    <div className="mx-auto mb-5 max-w-xl rounded-[12px] border border-ember bg-surface-2 px-4 py-3">
      <p className="text-sm">
        <span className="font-semibold">Finishing setup</span> — book your
        first class and you&apos;re all set.
      </p>
    </div>
  );
}
