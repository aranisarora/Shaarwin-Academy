/** Easily editable pricing — amounts in INR for the full booking period */
export const PRICING = {
  /** Price per group batch selected (covers 4-month booking window) */
  groupBatchPrice: 3000,
  /** Price per private slot selected (covers 4-month booking window) */
  privateSlotPrice: 6000,
};
